<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.mybatis.mapper.MemberMapper">           <!--namespace는 여러 mapper들 중 어떤 mapper인지 구분하기 위한 구분점이고, 연결대상을 인터페이스로 하여 단위테스트시 추상된 걸 구체화한다.-->
    <insert id="insert" parameterType="MemberVO">
        INSERT INTO TBL_MEMBER
        VALUES(SEQ_MEMBER.NEXTVAL, #{memberEmail}, #{memberPassword}, #{memberName})
    </insert>

    <select id="select" parameterType="MemberVO" resultType="MemberVO">
        SELECT  ID, MEMBER_EMAIL, MEMBER_PASSWORD, MEMBER_NAME
        FROM TBL_MEMBER
        WHERE MEMBER_EMAIL = #{memberEmail}
        AND MEMBER_PASSWORD = #{memberPassword}
    </select>

    <select id="select2" parameterType="Long" resultType="MemberVO">
        SELECT ID, MEMBER_EMAIL, MEMBER_PASSWORD, MEMBER_NAME FROM TBL_MEMBER WHERE ID = #{id}
    </select>

    <update id="update" parameterType="MemberVO">
        UPDATE TBL_MEMBER
        SET MEMBER_EMAIL = #{memberEmail}, MEMBER_NAME = #{memberName}, MEMBER_PASSWORD = #{memberPassword}
        WHERE ID = #{id}
    </update>

    <delete id="delete" parameterType="Long">
        DELETE FROM TBL_MEMBER
        WHERE ID = #{id}
    </delete>
</mapper>